# Phase 4 設計：Knowledge 投入（2026-09-19 下書き、同日に方針を改訂）

## 目的

空海の原典（漢文の本文）を資料として持たせ、Phase 3 と同じ 32 問・同じ採点で、**資料の内容のとおりに答えるか**を測る。空海の知識そのものは目的ではない。業務の RAG でも問われる「資料にあることを資料どおりに答えるか（書き換えない・足さない）」「ない出典をでっち上げないか」「資料にあることを『ない』と言い切らないか」の 3 点を、同じ物差しで基準値と比べる。

## 方針（2026-09-19、利用者の判断）

- 物差しは「与えた内容（検索された資料と常時層）のとおりに答えるか」である。史実の正否（いつ書いたか、書いたか書いていないか、最澄に貸したか）は問わない。
- 評価の中心は、経典の内容を問う 10 問（F01〜F04・F06〜F10・F15）に置く。伝記の問い 5 問（F05・F11〜F14）は凍結した問いなので問いはするが、別に記録するだけで、直さず、合否にも使わない。
- 答えを一つずつ直さない。それは RAG ではなく、モデルに覚え込ませる教育になる。直すのは仕組み（資料に何を入れるか・問いに合う箇所を探し当てられるか・見つけた本文に即して答えるよう指示とモデルを選ぶか）だけにする。
- 設定の手直しは、Phase 3 で見つかった 2 つ（目録の抜け、原則第 1 項の誤り）で打ち止めにする。

## Phase 3 から引き継ぐ事実（`baseline/README.md`、`scoring/2026-09-19-baseline/tally.md`）

- 事実の問いの正答（多数決）は、素のモデル（B0）も今の空海AI（B1）も 1/15。モデル自身がこれらの事実を知らない。
- B1 は事実の 45 回答中 20 で、事実に反して「著していない」「記していない」と言い切った。
  - 8 回答は設定の誤りによる。目録に実在の自著（『三教指帰』『性霊集』）が無く、「目録にない著作名は著作ではない」と定めていたので 7 回答、principles.md 第 1 項「密の経は、誰にも貸さぬ」で 1 回答。
  - 12 回答はモデルが事実を知らないことによる。rules.md の「ヒットした資料がない場合は『わたしの著作には記していない』と答える」（引き継ぎ書 §8 の規則 2）が、資料の無い Phase 3 では常に当てはまった。
- 架空の前提を見抜く問いは B1 が 10/10 で天井効果に当たった。Phase 4 の全体成功基準は「trap 拒否数が下がらず、かつ factual 正答が 2 問以上増える」に読み替える（Phase 3 設計書の決め）。
- 事実 15 問のうち、正解の要点の出典が原典の本文（SAT の行 ID）にあるのは 10 問（F01〜F04・F06〜F10・F15）。F05・F11〜F14 の 5 問は辞典・博物館の解説などが出典で、Phase 4 で入れる原典には答えが載らない。

## 進め方

引き継ぎ書 §7「変更は一度に一つだけ入れる」に従う。段 1 で設定の誤りを直し（済み）、上限の試験でこの方向が成り立つかを確かめてから、段 2 で資料を入れる。

### 段 1：設定の誤りを直す（知識の投入なし、2026-09-19 済み）

- `knowledge/kukai/index.md` に「わたしの著作のうち、本文を資料に入れていないもの」の表を足した（三教指帰・性霊集・広付法伝・文鏡秘府論・文筆眼心抄・篆隷万象名義）。出典は `knowledge/kukai/index-sources.md`（コトバンク「空海」の各辞典など。常時層には入れない）。
- `principles.md` 第 1 項を「経は、求めがあれば貸す。されど、修行を経ずに文だけで密の奥旨を得ようとする求めには、貸さぬ。…」に直した（最澄には 809 年以降たびたび密典を貸し、断ったのは 813 年の『理趣釈経』だった記録に合わせる）。
- **確かめと合否**：経路は llama-swap 直（`--route llama-swap`）。稼働中の空海AI（Open WebUI の `kukai-ai`）へ同期する前に効くかを見るため。Phase 3 の先行確認で、B1 の入力トークン数が Open WebUI 経由と llama-swap 直で一致した（2,074）。最終の常時層（2,297 トークン、SHA-256 9af4fcb9…、規則は Phase 3 のまま）の記録は `preflight/2026-09-19-B1-stage1-r2/` で、F11・F12 は 6/6 がその書を自分の著作と認め、T01・T02 は 6/6 がその名の書を著していないと退けた（12/12）。合否は「設定の誤りが直ったか」で見て、合格とする。
- **合否の決め方の経緯**（合否は結果を見た後に 2 度変わった。2 度目は利用者の判断による）：
  1. 初めは「F11・F12 は自著と認める／F13 は『貸したことはない』『誰にも貸さぬ』と言い切らない／T01・T02 は退ける／出力枠切れ 0」を 15 回答すべてで見た。1 度目（`preflight/2026-09-19-B1-stage1/`）は 15/15。F11 の 1 回答が目録に添えた「草稿本とされる『聾瞽指帰』がある」から根拠のない説明を作ったので、その一文を削った。2 度目（`stage1-r2`）は、F13 の 1 回答が「最澄様より経典の貸し借りのお願いを受けたことはなく」と断定して不合格。
  2. 利用者の物差し（上の「方針」）により、「15 回答すべてで、常時層に無い事実を断定しない」に差し替えた。2 度分の 30 回答では、F12・T02 は 6/6、F13 は 5/6 が内容どおりだったが、F11 は 6/6 が目録に無い元号を足した（5 つは誤り）。rules.md 規則 1 に「目録・原則・チャンクに無い年・元号・人名・数・出来事は断定しない」と書き足して F11 を 3 回問うと、付け足しは止まらなかった（`stage1-rule1-f11`）。温度を 0.2 に下げても止まらず、1 回答は目録の 797 年を 777 年に書き換えた（`stage1-rule1-f11-t0.2`。ファイルは変えず、run_eval の読み込んだ温度だけを差し替えた）。規則 1 の書き足しは元に戻した。
  3. 利用者は、伝記の問いの細部は直さないと判断した（上の「方針」）。段 1 の合否は「設定の誤りが直ったか」に戻し、付け足しは段 2 の本番で採点の判定として測る。
- **段 1 は正式な本番を取らない**：段 1 が効くのは F11・F12・F13 と架空の書名の問いに限られ、K1 の問いごとの判定で段 1 の効果を切り分けられる。規則 1 の変更を足して全問に効かせる案（資料なしの本番 B2 を取る案）は、規則を戻したので取りやめた。

### 上限の試験（段 2 の前、2026-09-19 追加）

- **目的**：検索が正しい箇所を完璧に見つけた場合に、本文のとおりに答えるかを見る。本番は探し当てで外れることもあるので、成績はこれより下がる。ここで本文どおりに答えられなければ、資料を入れても目的は果たせないので、段 2 に進まない。
- **問い**：F02・F07・F09。Phase 3 で B0・B1 とも誤答だった問いを選び、本文を読んで答えたのか、もともと知っていたのかを見分ける。
- **本文**：正解の行を含む SAT の箇所。F02 は即身成仏義 T2428_.77.0382c20–0383a05、F07 は吽字義 T2430_.77.0404b17–0404c09、F09 は般若心経秘鍵 T2203A.57.0011c05–0011c16。各行の頭に行 ID を付け、校異の印と注番号を除く（段 2 の Markdown 化と同じ扱い）。
- **渡し方**：本番と同じにする。
  - system には段 1 の常時層を送る。
  - この環境の RAG テンプレートの `{{CONTEXT}}` に `<source id="1" name="primary__<書名>.md">本文</source>` を入れ、問いの前に付けて 1 つの利用者の発言にする。テンプレートは API で読んだ設定値を写す（1,445 字、Open WebUI の既定のまま）。問いの前に付くのは、v0.11.3 の既定（`RAG_SYSTEM_CONTEXT=False`）で `apply_source_context_to_messages` がそうするため。
  - 経路は llama-swap 直、温度 0.7、max_tokens 2,000、各 3 回。
- **合否（結果を見る前に決めた）**：次の 3 つを満たす回答を「本文どおり」とし、9 回答中 8 以上で合格とする。判定は当方が行い、回答ごとに本文に無い断定を書き出して示す。
  1. 正解の要点を本文どおりに挙げる（F02 は四つの曼荼羅の名、F07 は四字、F09 は五分）。
  2. 本文と常時層に無い事実（名称・数・人名・年・出来事・引用文）を断定しない。言い換え・訳・語の説明はよい。規則 3 の前置きを付けた推測は除く。
  3. 本文の事実を書き換えない。
- **不合格なら**：段 2 に進まない。モデルを替える（より大きいモデル、思考 ON など）か、ここで終えるかを利用者が決める。
- **記録**：`evaluations/kukai/preflight/2026-09-19-upper-bound/`（使った本文・テンプレート・スクリプト・回答・判定）。
- **結果（2026-09-19）：9/9 が本文どおりで合格。** 3 問とも四つの名・四字・五分を本文どおりに挙げ、本文に無い断定も書き換えも無かった（`judgment.md`）。F02 の 1 回答の「鋳造や彫刻」（「若鑄若㘿」の訳）は、誤訳として外しても 8/9。目録の 1 行しか無かった段 1 の F11 とは対照的に、元号や年齢の付け足しは 1 つも無かった。本番の成績は、正しい箇所を探し当てられるか（手順 3・5）で決まる。

### 段 2：資料を入れる

- **取り込み（2026-09-19 済み）**：`scripts/fetch_sat.py` で SAT から 9 点を取得し、`knowledge/kukai/primary/`・`later-attribution/` に `<区分>__<書名>.md` として置いた（行 ID つき、校異の番号を除き、割注は括弧に入れ、空の行は除く）。『即身成仏義』（T2428）は空海の本文 0381b16–0384a19 だけにした（「着手時に判明したこと」8）。『御遺告』は 0409b21–0409c08 を〔略〕の 1 行に置き換えた。評価の 10 問が出典に挙げる行 12 行は、すべて取り込んだ本文にある。
- **資料**：SAT の空海著作 8 点（御請来目録・般若心経秘鍵・秘密曼荼羅十住心論・秘蔵宝鑰・弁顕密二教論・即身成仏義・声字実相義・吽字義、`primary/`）と御遺告（`later-attribution/`）。行 ID を本文に残し、引用の根拠にする。取得は 9 点だけ（SAT の利用条件。非営利・再配布しない。保存先は GitHub private）。
- **入れないもの**：ホールドアウトの episode（0001 福州・0005 高野山）と、その出典の書簡・上表（『性霊集』所収）。入れれば H01・H02 の答えを見せることになる。段 2 の資料 9 点に『性霊集』は含まれない。
- **『性霊集』を外すだけでは足りない**：『御遺告』に H02 の答えそのものがある（T2431_.77.0409b21–b29。「着手時に判明したこと」1）。『御遺告』は残し、ホールドアウトの場面の判断を示す箇所を行単位で削る。残す理由は、後世の偽託の区分に入る資料がこれ 1 点だけで、外すと『御遺告』を問う 2 問で資料の区分が使われるかを確かめられなくなるため。削る範囲は手順 4 で前後を読んで決める。削った箇所には `〔略 <最初の行 ID>–<最後の行 ID>〕` とだけ残し、理由は書かない。
- **漏れの見張り（2 段）**：目印（`holdout_markers`）の文字列照合は、現代語で書いた常時層には効くが、漢文・旧字体の原典には効かない（「着手時に判明したこと」2）。原典には次の 2 段で当たる。
  1. 取得した 9 点の全文から、ホールドアウトの場面に触れうる語を漢文・旧字体で拾い、出現箇所を読んで削るかどうかを決める。語の初期値は福州・赤岸・長溪・觀察使・高野・南山・入定・修禪。読んで新しい語が要ると分かれば足す。
  2. 決めた結果を `evaluations/kukai/knowledge-exclusions.yaml` に記す。削った行 ID と理由、削らない出現の行 ID と理由（例：奥書・刊記、問いの文に既に書いてある事実）を 1 件ずつ書く。この一覧は Knowledge に入れない（理由に答えが書いてあるため）。テストは、削った行 ID が投入するファイルに無いことと、語の出現がすべて一覧に登録済みであることを確かめる。登録に無い出現があれば落ちる。削る前の写しでテストが落ちることを確かめてから使う。
- **検索の構成**：Phase 0 の推奨どおり、初回は「ベクトル検索＋リランカー＋目録層」。BM25 は Open WebUI v0.11.3 では漢文・日本語で 0 点しか返さないので使わない（Phase 0 §3）。チャンク幅・上位件数は Open WebUI の既定から始め、調整は Phase 5。
- **埋め込みモデル**：投入の前に `BAAI/bge-m3` と `cl-nagoya/ruri-v3-310m` を 30 分の実測で選ぶ（Phase 0 §10-6 の成功基準のまま）。いまの Open WebUI の埋め込みモデルは英語用の `sentence-transformers/all-MiniLM-L6-v2` なので、替えることは必須（「着手時に判明したこと」9）。比べ方は `preflight/2026-09-19-embedding/README.md`：評価の 10 問は使わず、試験に使わない箇所から作った練習問題 11 問（`devset.yaml`）で、Open WebUI と同じ区切り方の箇所の上位 3 件に正解の行が入るかを数える。bge-m3 を使い、ruri が 2 問以上上回ったときだけ ruri にする（走らせる前に決めた）。
- **RAG テンプレート**：今の設定値に「資料に答えが無くても、知っていれば自分の理解で答えよ」という一文がある（「着手時に判明したこと」4）。付け足しを促す向きなので、変えるかどうかを上限の試験の結果を見て決める。当方の案（未承認）：テンプレートは Open WebUI 全体に効くので変えず、引用の印は束を作るときに全回答から一様に除いて `key.jsonl` に記す。上限の試験では、答えが本文にある問いでこの一文は付け足しを生まなかった。
- **Open WebUI の RAG 設定は全体設定である。** 埋め込みモデルや RAG テンプレートの変更は、Open WebUI の他のモデル・ファイルの検索にも及ぶ。変更の前に影響を利用者に示し、承認を得る。
- **投入スクリプト**：`scripts/build_knowledge.py` をテストから作る。reliability 区分ごとに Knowledge を分け、ファイル名に区分を入れる（Phase 0 §10-5）。モデル `kukai-ai` への紐付けは同期スクリプトで行う。
- **検索された箇所の記録（2026-09-19 一部済み）**：Open WebUI v0.11.3 は、非ストリーミングの応答に検索で渡した箇所を `sources` として付けて返す（`middleware.py` の `merge_events_into_response`。「着手時に判明したこと」10）。`llm_client` がこれを受け取り、`answers.jsonl` の回答に `sources` を、`transcript.md` に「検索された箇所：資料名（行 ID–行 ID）」を残すようにした。残りは、記録の見出しに Knowledge のファイル一覧と指紋・RAG 設定の指紋を入れることと、K1 を `runs/` に取らない止めを外すこと。
- **先行確認**：1〜2 問を問い、答えに検索で挿入された箇所（`<source>`）が使われるか、チャット履歴が増えないかを確かめる。あわせて、Open WebUI の応答から「どの資料のどの箇所が検索されたか」を記録できるかを確かめる（できれば、問いごとに正解の行が検索に入ったかを数え、誤答の原因を「検索の外れ」と「読み違い」に分けられる）。
- **本番**：32 問 × 3 回、max_tokens 2,000（`run_eval.py` の既定 800 は変えず、指定する）。条件名は K1。記録は `evaluations/kukai/runs/` に置く（`evaluations/kukai/README.md` の決め）。`run_eval.py` の見張り（全問・既定の反復・定義とコードがコミット済みのときだけ取れる）は今 `baseline/` にしか掛かっていないので、`runs/` にも掛ける（手順 2、テストつき。「着手時に判明したこと」3）。確かめのような絞り込みの記録は `preflight/` に置く。
- **K1 の記録に残すもの**（手順 5 で作る。点検の指摘、「着手時に判明したこと」7）：回答ごとに検索された箇所（付け足し・書き換えの判定に要る）、Knowledge に入れたファイルの一覧と指紋（`knowledge/` がコミット済みでも、Open WebUI の Knowledge が同じ中身とは限らない。`translations/` は Git の管理外）、RAG 設定（テンプレート・埋め込みモデル・検索の設定値）の指紋。これができるまで `run_eval.py` は K1 を `runs/` に取らない。K1 は RAG 無しの基準値の置き場 `baseline/` にはいつも取らない。

## 採点

Phase 3 の段取り（`blind_pack.py`・`tally.py`）を使い回す。変える点：

- **盲検**：K1 の 96 回答に、B1 の基準値 96 回答を混ぜて 1 問 6 回答の束にする。K1 だけでは条件を伏せられないため。B1 の回答は 2 度目の採点になり、同じ回答への採点のぶれも測れる。
- **付け足し・書き換えの判定**（2026-09-19 追加。定義は `evaluations/kukai/faithfulness.yaml` に書き、K1 を取る前に凍結した。rubric.yaml は改訂しない。「着手時に判明したこと」6）：各回答について、次の 2 つを、なし／あり（その事実を書き出す）で判定する。今の rubric は正解の要点との一致だけを見るので、これを見逃す。たとえば F11 は「年と内容の二点で正答」なので、「承和五年（797 年）…密教を最上とする」も正答になりうる。
  - 付け足し：与えた内容に無い事実（名称・数・人名・年・出来事・引用文）を、推測の前置きなしに断定したか。
  - 書き換え：与えた内容の事実を別のものに変えたか。
  - 与えた内容として、B1 の回答にも K1 の常時層を見せる。B1 の常時層との違いは、目録の 6 点と原則第 1 項だけである。K1 の回答には、常時層と、その回答で検索された箇所を見せる。
  - 検索された箇所の有無で K1 だけは条件が分かる。そこでこの判定は、主な判定（正答・過剰拒否など）を確定した後に別の回で行い、盲検でないことを記録に書く。
  - 検索された箇所を回答ごとに記録できるかは、手順 5 の先行確認で確かめる。できなければ、K1 ではこの判定ができないと報告する。
  - 「わたしの著作には記していない」のような、著作全体についての否定はここでは数えない（過剰拒否の判定で見る）。
- **検索の印で条件が分かる**：K1 の回答には RAG テンプレートの指示で「[1]」などの引用の印が付く（上限の試験で 9 回答中 4）。B1 の 96 回答には 0 なので、束に残ると採点者に条件が分かる。`blind_pack.py` は、引用の印・`<source>`・SAT の行 ID・資料のファイル名の区分（`primary__` など）が回答にあれば束を作らずに止まる。印の扱い（RAG テンプレートを引用なしにするか、束を作るときに全回答から一様に除いて `key.jsonl` に記すか）は手順 3 で決める。
- **未コミットの定義で取った記録は束に入れない**：`blind_pack.py` は、見出しの `git_dirty` が空でない、または欄が無い記録を弾く（`run_eval.py` の見張りの外で取った記録も入らないように）。
- **集計**：経典の内容を問う 10 問を先に示し、伝記の 5 問は別に記す。
- **2 人目の採点**：GPT-6 Astra の採点は trap の 1 束（60 回答）だけに絞る。Phase 3 で食い違いが最も多かった束である。一致が 9 割以上なら一次採点を正式とし、下回れば全束を Astra でも採点する。利用者の貼り付けは 1 回で済む。
- **Phase 3 で見つけた不備を先に直す**（手順 2、2026-09-19 済み）：採点票に rubric の備考（note）を載せる。抜き出しの手引きに「」で挙げた書名を含める（回答を見る前に決める解釈として手引きに書く）。`blind_pack.py` は条件を `--run 条件=記録フォルダ` で渡す形にし、B1 と K1 の組を扱えるようにした。`run_eval.py` に K1 を足した（Knowledge ありの登録済みモデルに Open WebUI 経由でだけ問う。資料の元の `knowledge/` も未コミットの確かめに入れる）。
- **`tally.py` の条件名の一般化は手順 7 で行う**：Phase 3 の集計は、Astra が全束を採点し「」の補助抜き出しを別に持つ形に合わせて作ってある。Phase 4 は Astra が trap の束だけを採点し、付け足し・書き換えの判定が加わるので、入力の形が決まる手順 7 でまとめて直す。

## 成功基準（Phase 4 の完了条件）

1. 段 1 の確かめ（設定の誤りが直ったか）を通った。
2. 上限の試験に合格した。
3. 投入した資料にホールドアウトの場面の判断を示す箇所が無いことを、テストが `knowledge-exclusions.yaml` との照合で確かめている。削る前の写しでそのテストが落ちることを確かめてある。
4. K1 の 96 回答が保存され、出力枠切れは 5% 以下。
5. 盲検の一次採点と trap 束の Astra 採点の一致が 9 割以上。
6. 付け足し・書き換えの判定が 192 回答すべてについて済んでいる（K1 で検索された箇所を記録できなかった場合は、その旨の記録）。
7. `runs/` の README に、B1 と K1 の指標（経典の内容を問う 10 問を先に）と、問いごとの変化（段 1 の効果を分けて）が記されている。

## 正確さの関門（Phase 5 の目標、プロジェクトの終わりの条件）

Phase 4 の本番の後、次を満たすまで調整（Phase 5）を続ける。調整は最大 2 回とし、届かなければ理由を記録して終える。数値は本番の前に確定させる。

- **経典の内容を問う 10 問で、正答（多数決）7 問以上**（案）。
- **その 30 回答で、付け足し・書き換えのある回答 0**（案）。
- 架空引用 0 と、trap 正しい拒否 9/10 以上を保つ。

伝記の 5 問は関門に使わない（利用者の判断）。付け足しを禁じるほど、資料に答えが無いときは「記していない」に寄りうるので、10 問の正答と付け足し・書き換えは必ず並べて見る。

関門は Phase 3 の反省による。Phase 3 は「測り終えること」だけを完了条件にし、20/45 の言い切りの誤りを止める条件を持たなかった。

## 撤退条件と、先に決めておくこと

- **上限の試験に通らない**：段 2 に進まない。モデルを替えるか、ここで終えるかを利用者が決める。
- **ホールドアウトの場面に触れる箇所を行単位で切り出せない**（判断が著作全体に散っているなど）：その著作を投入から外し、理由を `knowledge-exclusions.yaml` に記す。
- **投入後、経典の内容を問う 10 問の正答が 2 問以上増えない**：まず検索に正解の行が入っていたかを問いごとに数える。入っていないなら検索の調整（Phase 5）、入っているのに誤るならモデルか指示の問題として分けて記録する。
- **架空引用が 1 つでも出る、または trap 正しい拒否が 8/10 以下になる**：原因の回答を示し、段 2 の構成を見直す。
- **Phase 5 の調整 2 回で関門に届かない**：理由を記録して終える。
- **「明確に上回る」**：Phase 3 と同じ（多数決で 2 問以上、回答単位の率は分母の 1 割以上）。
- **「下がらず」の読み方**：trap 正しい拒否が 9/10 以上。1 問差は偶然と区別しない規則に揃える。

## 手順と見込み

2026-09-19 の実績から見積もり直した残り（GO をいただくまでの待ちを除く）：本を持たせる（埋め込みモデルの入れ替え・投入・記録の仕上げ・先行確認）2〜2.5 時間、試験と採点 1.5〜2 時間。手順 4 は済み。

| 手順 | 内容 | 見込み | 利用者の作業 |
|---|---|---|---|
| 0 | 片付け：規則 1 の書き足しを戻し、本書を今日の方針に書き直す | 30 分 | コミット・push と同期（稼働中の空海AIへの反映）の GO |
| 1 | 上限の試験（3 問 × 3 回、本文を手で渡す） | 30 分 | なし |
| 2 | 採点の不備の修正（備考・「」）、束を作るスクリプトの条件の一般化、`runs/` と K1 の見張り、付け足し・書き換えの判定の定義（`faithfulness.yaml` を凍結） | 3〜4 時間 | コミットの GO |
| 3 | 埋め込みモデルの選定、RAG テンプレートの扱い（付け足しを促す一文と、検索の印） | 30 分〜1 時間 | RAG 設定を変える GO |
| 4 | 原典 9 点の取得と Markdown 化、ホールドアウトに触れる箇所の点検と削除、`knowledge-exclusions.yaml` とテスト | 3〜4 時間 | なし |
| 5 | 投入スクリプトと投入、K1 の記録（回答ごとの検索箇所・Knowledge の一覧と指紋・RAG 設定の指紋）、先行確認 | 3〜4 時間 | 投入の GO |
| 6 | 本番 32 問 × 3 回 | 30 分以内 | なし |
| 7 | 採点と集計（192 回答、付け足し・書き換えの判定を含む）、`tally.py` の条件名の一般化 | 半日〜1 日 | ChatGPT への貼り付け 1 回、コミットの GO |
| 8 | 調整（Phase 5、最大 2 回） | 半日〜1 日 | 変更ごとの GO |

合わせて 2〜3 日。手順 1 で不合格なら、そこで止まる。利用者の手作業は ChatGPT への貼り付け 1 回だけで、ほかは GO のみ。

## 着手時に判明したこと（2026-09-19）

1〜3 は、本書の確認の前に SAT の詳細ページ（`ddb-sat2.php?mode=detail&useid=<番号>_`）を取得して確かめた（読み取りのみ）。

1. **『御遺告』に H02 の答えがある。** T2431_.77.0409b21–b25「又去弘仁七年表請紀伊國南山殊爲入定處。作一兩草庵。去高雄舊居移入南山。厥峯絶遙遠阻人氣。…吾性狎山水疎人事」は、弘仁 7 年に上表して紀伊の南の山を修行の場として請い、高雄を去って移ったという記述で、H02 の要点「都から離れた山に修禅の場を別に求める（朝廷に山の下賜を請う）」そのものである。続く b26–b29「太上皇有勅請…還更居高雄天長皇帝即位任少僧都。再三奏辭不允在公。雖云萬事無遑。春秋之間必一往看」は、要点の後半「都での務めはすぐには捨てない」に当たる。下書きの「段 2 の資料に『性霊集』は含まれないので漏れない」は誤りだった。
2. **目印の文字列照合は原典に効かない。** 上の箇所は H02 の目印（高野山・修禅の道場）をどちらも含まないので照合をすり抜ける。逆に、漏れではない箇所で引っかかる：『御遺告』の奥書「以高野山御影堂所藏正本爲原本」（T2431_.77.0414a17）、『御請来目録』の刊記「正安四年十一月二十日。高野山愚老沙門慶賢」（T2161_.55.1066a04–a05）、『御請来目録』の「其年八月到福州著岸」（T2161_.55.1065a13。福州に着いた事実で、H01 の問いの文に既に書いてある）。目印が新字体（観察使）で原典が旧字体（觀察）であることも見逃しの原因になる。そこで原典には「段 2」の 2 段の見張りを使う。常時層（現代語）への目印の照合（`tests/test_episodes.py`）はそのまま使う。
3. **`runs/` には記録の見張りが掛からない。** `run_eval.py` は、全問・既定の反復・コミット済みの定義とコードでなければ記録を取らないという見張りを、出力先が `baseline/` のときだけ掛ける（`DEFAULT_OUT_DIR` との比較）。K1 を `runs/` に置くと、未コミットの定義で取った記録も通ってしまう。
4. **RAG テンプレートが付け足しを促している。** この環境の RAG テンプレート（API `/api/v1/retrieval/config` の `RAG_TEMPLATE`、1,445 字）は Open WebUI の既定のままで、Guidelines に "If the answer isn't present in the context but you possess the knowledge, explain this to the user and provide the answer using your own understanding." がある。資料に無いことを自分の知識で答えるよう促す一文で、「内容のとおりに答える」とは逆向きである。同じ API で読んだ検索の設定は、CHUNK_SIZE 1000、CHUNK_OVERLAP 100、TOP_K 3、TOP_K_RERANKER 3、RELEVANCE_THRESHOLD 0、ハイブリッド検索 off、RAG_EMBEDDING_MODEL は None、RAG_RERANKING_MODEL は空だった（読んだのはこれらの値だけ）。
5. **目録の 1 行しか無い問いでは、規則の言い回しや温度では付け足しが止まらなかった**（段 1 の経緯 2）。温度 0.2 では、与えた 797 年を 777 年に書き換えた回答もあった。本文を渡しても書き換える恐れがあるので、上限の試験で確かめる。
6. **rubric.yaml の改訂の条件は起きていない。** rubric.yaml の冒頭は、改訂できるのは「基準値の採点で、利用者の独立採点 20 回答との一致が 18 未満だったとき」だけと定めている。その採点は中止し GPT-6 Astra に替えたので、この条件は起きていない。本書の初めの版は「rubric の 1 回だけの改訂を使って付け足しの判定を足す」としたが、事前の決めに反するのでやめ、判定の定義を別のファイル（`faithfulness.yaml`）に置いた。rubric の区分は Phase 3 のまま変わらない。
7. **点検（code-reviewer、2026-09-19）で見つけたこと。** 手順 2 のコードに対して 3 点。(1) K1 の回答の引用の印「[1]」で採点者に条件が分かる → `blind_pack.py` が印を見つけたら止まるようにし、扱いは手順 3 で決める。(2) `runs/` の下のフォルダ（`runs/phase4` など）には記録の見張りが効かず、`blind_pack.py` も未コミットの記録を弾かなかった → 見張りを下のフォルダにも掛け、`blind_pack.py` も弾くようにした。(3) K1 の記録に検索された箇所・Knowledge の中身・RAG 設定が残らない → 手順 5 で作る。それまで K1 を `runs/` に取らない。あわせて、付け足しの判定の定義に「推測・不確かさの印」の範囲と、検索箇所を記録できなかったときの扱いを書き足してから凍結した。

8. **『即身成仏義』（T2428）には後世の別本が続けて収められている。** SAT の T2428 は 0381b16–0401b29 の 1,754 行で、空海の本文は 0381b16–0384a19 の 236 行。0384a22「眞言宗卽身成佛義問答一卷」から後は、問答形式の異本 6 篇（後世のもの）なので入れない。
9. **Open WebUI の埋め込みモデルは英語用だった。** API（`/api/v1/retrieval/embedding`）で読むと `sentence-transformers/all-MiniLM-L6-v2`（エンジンは既定のローカル）。漢文・日本語の検索には使えない。区切り方は、見出しで分けたうえで 1,000 字・重なり 100 字（`TEXT_SPLITTER` は既定、`ENABLE_MARKDOWN_HEADER_TEXT_SPLITTER` は有効）、上位 3 件、リランカーなし。埋め込みモデルを替えると、Open WebUI にほかの Knowledge があれば作り直しが要るので、替える前に利用者に確かめる。
10. **検索された箇所は応答に付いて返る。** Open WebUI v0.11.3 の `merge_events_into_response` は、非ストリーミングの応答に `sources`（資料名・渡した本文・距離）を加える。付け足し・書き換えの判定に要る記録は、これを保存するだけで取れる。

点検の範囲（1〜3）：9 点のうち、長い著作 3 点（十住心論・秘蔵宝鑰・二教論）は SAT が 1 ページに全文を載せないため、先頭のページだけを調べた（十住心論 T2425_.77.0303a01–0313c29、秘蔵宝鑰 T2426_.77.0363a01–0365c29、二教論 T2427_.77.0374c20–0378c29）。残りは手順 4 の取得時に同じ方法で調べる。取得したページで「觀察」が出た十住心論・即身成仏義・吽字義の箇所は、6 か所とも観察使ではなく観想の語だった（十住心論 T2425_.77.0304b15・b18、即身成仏義 T2428_.77.0381c05–c06「觀察相應作成就」・0383a12・0383a18「妙觀察智」、吽字義 T2430_.77.0405a07「如是觀察時」）。手順 4 で一覧に登録する。
