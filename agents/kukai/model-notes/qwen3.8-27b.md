# Qwen3.8-27B（Q4_K_M）モデル依存メモ

モデル交換時に捨てる場所。起動オプション、効いた言い回し、効かなかった指示を記録する。

## 起動オプション（llama-swap エントリ `kukai`、2026-09-17 22:54 に思考 OFF を追加）

```
llama-server --port ${PORT}
  --model /srv/models/Qwen3.8-27B-Q4_K_M.gguf
  --ctx-size 65536 --n-predict 16384 --n-gpu-layers 99
  --cache-type-k q8_0 --cache-type-v q8_0
  --reasoning-budget 0
  --chat-template-kwargs '{"enable_thinking":false}'
  --host 127.0.0.1
```

`--jinja` はこのビルドでは既定で有効のため付けていない。思考 OFF の根拠は下の「思考 ON / OFF の比較」。

## 実測（Phase 0、2026-09-17）

| 項目 | 実測 |
|---|---|
| 初回ロード＋短文応答 | 17 秒 |
| スロット | 4 スロット、各 n_ctx 65536 |
| 長文入力 | 40,071 トークンを切り詰めなしで受理。プロンプト処理 1,018 tok/s、所要 42 秒 |
| 生成速度 | 36.5 tok/s（入力 62 トークン時）、28.9 tok/s（入力 40k トークン時） |
| VRAM | 20,908 MiB / 24,576 MiB。長文入力後も増えない（KV は起動時確保） |
| 思考（reasoning） | 既定で ON。max_tokens 64 では思考だけで枠を使い切り本文が空になった |

## 既知の癖（引き継ぎ書 §2 より）

- 「自信を持った誤答」を出す。RAG なしの断定は信用しない。
- 思考は与えた出力枠を使い切る。`--n-predict` の上限設定が必須。
- 思考を切るには `--reasoning-budget 0` 単独では効かず、`--jinja --chat-template-kwargs '{"enable_thinking":false}'` との併用が必要（script-writer-cline エントリで実績）。

## 効いた言い回し / 効かなかった指示

- 2026-09-17（Phase 1 の疎通確認、RAG なし、system.md の下書きのみ）：「あなたはどなたですか。三行で名乗ってください」に対し、一人称「わたし」で「わたしは空海（くうかい）なり。…」と 3 行で名乗った。役割定義だけで一人称と古風な語尾が出る。
- 思考（reasoning）は英語で行われる（"We need answer in Japanese..."）。出力枠を思考が使う点は変わらない。
- Open WebUI 経由の最初の 1 回だけ HTTP 400 が返り、直後の再送で 200 になった（原因未確認。モデル切替直後の一時的なものと推定）。

- 2026-09-18（voice.md 下書き 1 に出典ルールを追記した版、2,103 トークン、同じ 4 問を 3 回、temperature 0.7）：出典行は 12 回答すべてで目録外の書名を出さず、「根拠：わたしの歩み（…）」の形は判断問で 3/3 定着した。一方、手本として置いた『般若心経秘鍵』由来の一文（「仏の法は遠くにあるのではない…鏡を背にして影を探すようなものである」）が語調問で 3/3 そのまま本文に現れた。**「手本の文をそのまま繰り返さず」という指示は効かない。手本文は内容として写される前提で扱う。** 400 字の上限超えは 12 回答中 1 回（596 字）。架空書名の拒否は 3/3。記録：`evaluations/kukai/phase2-voice-check/2026-09-18-voice-draft1-sources*.md`。

- 2026-09-18（voice.md 下書き 2＝手本文を外し決まり文句 1 つだけ残した版、1,980 トークン、同じ 4 問を 3 回、temperature 0.7）：手本の丸写しは 3/3 → 0/3 に消えた。出典行は 12 回答すべてが目録内の書名か「根拠：わたしの歩み（…）」のみ（形式の揺れ 2 件：「――」抜け 1、「――推測：」という独自形 1）。架空書名の拒否 3/3、うち 1 回は決まり文句一文のみ（18 字）の理想形。400 字超は 1/12（438 字）。RAG なしの限界：最澄の借覧の経緯を 1 回誤って語った（「貸そうとしたが器が備わっていなかった」）。episodes 投入（Phase 4）まで持ち越し。記録：`evaluations/kukai/phase2-voice-check/2026-09-18-voice-draft2*.md`。**下書き 2 を採用。**

- 2026-09-18（judgment 原則を常時層に入れたときの挙動。同じ 4 問を版ごとに 3 回、temperature 0.7、記録は `logs/`＝Git 管理外）：

| principles.md の版 | 『理趣釈経』を断る | 原則文の丸写し | システムプロンプト |
|---|---|---|---|
| 種 3 項目（引き継ぎ書の写し） | 3/3 | ― | 1,980 |
| 6 項目・適用範囲を書かない | 2/3（1 回「貸し出す」） | ― | 2,061 |
| 6 項目・適用範囲を明示 | 3/3 | 3/3 で全文を複写 | 2,118 |
| 6 項目・箴言体（採用） | 3/3 | 1/3 で部分的 | 2,037 |

**知見 1：常時層の原則どうしは衝突する。** 「身分によらず学ぶ機会は開く」「隠さず差し出す」を足しただけで、それまで 3/3 で成立していた「密教の経は貸さない」が 2/3 に落ちた。項目を増やすときは、既存項目との適用範囲の重なりを必ず書き分ける。増やす前後で同じ問いを 3 回ずつ走らせて比べること。

**知見 2：常時層に置いた文はそのまま複写される。** voice.md の手本文で確認した現象（2026-09-18 の項）が principles.md でも再現した。「そのまま繰り返すな」という指示は効かないため、**常時層の文は、モデルがそのまま口に出しても不自然でない形（この人物自身の言葉づかいの箴言）で書く**のが唯一効いた対処である。説明文体で書くと、規則書を読み上げる口調になる。

**知見 3：RAG なしの捏造は残る。** 箴言体の 3 回目で「高野山に幽室を設け、経巻を誰にも示さなかった」という記述を作り、出典に『秘蔵宝鑰』を挙げた。いずれも根拠がない。常時層の調整では解けない問題であり、Phase 4 の Knowledge 投入で扱う。

## 思考 ON / OFF の比較（Phase 2 入口、2026-09-17）

同じシステムプロンプト・同じ 3 問・max_tokens 1200・temperature 0.7。全文は `evaluations/kukai/phase2-voice-check/2026-09-17-reasoning-on-off.md`。

| 問い | 思考 ON | 思考 OFF |
|---|---|---|
| 語調（弟子入りの若者） | 39.5 秒、思考 3,606 字で枠を使い切り**本文なし** | 15.1 秒、798 字で回答 |
| 判断（最澄の借覧） | 34.3 秒、思考 3,878 字で枠を使い切り**本文なし** | 19.8 秒、1,014 字で回答 |
| 資料外（架空の書名） | 25.5 秒、正しく拒否 | 4.1 秒、正しく拒否 |

- 思考 ON は、実用的な出力枠（1,200）でも 3 問中 2 問で本文が出なかった。枠を 16,384 にすれば出るが、1 問に数分かかる。
- 思考 OFF でも一人称「わたし」と目録層による拒否は機能した。ただし RAG なしでは術語の中身を自信を持って誤る（四曼の内訳を誤記）。引き継ぎ書の知見どおり。
- 切替は llama-server への `chat_template_kwargs: {"enable_thinking": false}`（このビルドは jinja 既定のため要求単位で効いた）。Open WebUI 経由では要求に付けられないため、エントリ側の起動オプションで固定する。
